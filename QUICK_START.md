# 🚀 Быстрый запуск проекта

## ✅ Текущий статус

- [x] База данных создана (`heritage_routes`)
- [x] Схема применена (4 таблицы)
- [x] Данные импортированы (4230 объектов)
- [ ] Backend запущен
- [ ] Frontend запущен

---

## 🎯 Запуск Backend (3 простых команды)

Откройте PowerShell или CMD и выполните:

### 1. Перейдите в папку backend

```powershell
cd C:\Users\vladi\OneDrive\Documents\GitHub\curse_project_3_sem\backend
```

### 2. Проверьте зависимости

```powershell
python check_and_run.py
```

Если увидите ошибку "НЕ УСТАНОВЛЕН", выполните:

```powershell
python -m pip install -r requirements.txt
```

### 3. Запустите сервер

После успешной проверки, запустите:

```powershell
python main.py
```

**ИЛИ еще проще - двойной клик на файл:**
```
run_backend.bat
```

---

## ✅ Ожидаемый результат

```
============================================================
🚀 Запуск Heritage Routes System v1.0.0
============================================================
✓ Пул соединений с MySQL создан
✓ Подключение к базе данных успешно
✓ Сервер запущен на http://localhost:8000
✓ Документация API: http://localhost:8000/docs
============================================================
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

---

## 🌐 Откройте в браузере

- **API Documentation:** http://localhost:8000/docs
- **Health Check:** http://localhost:8000/health
- **Main Page:** http://localhost:8000

---

## 🎨 Запуск Frontend

После запуска Backend, откройте **НОВЫЙ терминал**:

```powershell
cd C:\Users\vladi\OneDrive\Documents\GitHub\curse_project_3_sem\frontend
python -m http.server 5500
```

Затем откройте: **http://localhost:5500/login.html**

**Логин:** `admin`  
**Пароль:** `admin123`

---

## ❗ Возможные проблемы

### 1. "ModuleNotFoundError: No module named 'fastapi'"

**Решение:**
```powershell
cd backend
python -m pip install -r requirements.txt
```

### 2. "MySQL не подключается"

**Решение:**
- Откройте XAMPP Control Panel
- Убедитесь что MySQL запущен (зеленый индикатор)
- Проверьте в phpMyAdmin что база `heritage_routes` существует

### 3. "Порт 8000 занят"

**Решение:**
```powershell
# Найдите процесс
netstat -ano | findstr :8000

# Завершите процесс (замените PID на найденный)
taskkill /PID <PID> /F
```

### 4. Несколько версий Python

Если у вас установлено несколько Python, используйте полный путь:

```powershell
# Проверьте версию
python --version

# Или используйте конкретную версию
py -3.13 main.py
```

---

## 🔄 Полный перезапуск

Если что-то пошло не так:

```powershell
# 1. Остановите сервер (Ctrl+C если запущен)

# 2. Переустановите зависимости
cd backend
python -m pip uninstall -y fastapi uvicorn mysql-connector-python
python -m pip install -r requirements.txt

# 3. Проверьте
python check_and_run.py
```

---

## 📊 Проверка что всё работает

### Backend

```powershell
curl http://localhost:8000/health
```

Должно вернуть:
```json
{
  "status": "healthy",
  "database": "connected",
  "version": "1.0.0"
}
```

### Frontend

Откройте http://localhost:5500/login.html и войдите с учетными данными `admin/admin123`

---

## 🎉 Успех!

Если всё работает:
1. ✅ Backend отвечает на http://localhost:8000
2. ✅ Frontend открывается на http://localhost:5500
3. ✅ Вход работает
4. ✅ Можно строить маршруты на карте

**Наслаждайтесь приложением!** 🗺️🏛️

