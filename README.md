# Система планирования культурных маршрутов

**Веб-приложение для построения туристических маршрутов по объектам культурного наследия города Москвы**

## 📋 Описание проекта

Система предназначена для автоматизированного формирования пешеходных и туристических маршрутов по объектам культурного наследия на основе открытых государственных данных города Москвы.

## 🛠 Технологии

### Backend
- Python 3.10+
- FastAPI
- MySQL 8.x
- PyJWT (авторизация)
- bcrypt (хэширование паролей)

### Frontend
- HTML5 + CSS3
- JavaScript (Vanilla)
- Leaflet.js (интерактивная карта)
- Fetch API

### База данных
- MySQL 8.x с Spatial индексами
- Типы данных: POINT, GEOMETRY
- Функции: ST_Distance_Sphere, ST_Within, ST_Buffer

## 📁 Структура проекта

```
curse_project_3_sem/
├── backend/              # Backend API (FastAPI)
│   ├── routes/          # API эндпоинты
│   ├── services/        # Бизнес-логика
│   └── requirements.txt
├── frontend/            # Web интерфейс
│   ├── css/            # Стили
│   └── js/             # JavaScript
├── database/           # SQL схема и импорт данных
├── data.json          # Датасет объектов наследия
└── meta.json          # Паспорт датасета
```

## 🚀 Установка и запуск

### Требования
- Python 3.10+
- MySQL 8.x (XAMPP)
- Современный браузер

### 1. Настройка базы данных

Запустите MySQL через XAMPP и создайте базу данных:

```sql
CREATE DATABASE heritage_routes CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Примените схему:

```bash
mysql -u root -p heritage_routes < database/schema.sql
```

### 2. Импорт данных

```bash
cd database
python import_data.py
```

### 3. Запуск Backend

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: http://localhost:8000

### 4. Запуск Frontend

Откройте файл `frontend/index.html` в браузере или используйте любой HTTP сервер:

```bash
cd frontend
python -m http.server 5500
```

Frontend будет доступен по адресу: http://localhost:5500

## 📖 API Документация

После запуска backend документация доступна по адресам:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Основные эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| GET | /api/objects | Получить список объектов культурного наследия |
| POST | /api/route | Построить маршрут от заданной точки |
| POST | /api/register | Регистрация нового пользователя |
| POST | /api/login | Авторизация пользователя |
| GET | /api/routes | Получить историю маршрутов пользователя |

## 💡 Функционал

- 🗺️ Интерактивная карта Москвы
- 📍 Выбор точки старта кликом по карте
- 🎯 Автоматическое построение оптимального маршрута
- 📊 Настройка количества объектов в маршруте
- 🏛️ Подробная информация о каждом объекте наследия
- 👤 Система регистрации и авторизации
- 📝 История построенных маршрутов
- ⚡ Кэширование для быстрой работы

## 🎓 Автор

Курсовой проект 3 семестр

## 📄 Лицензия

Данные: Открытые данные города Москвы (https://data.mos.ru/)

