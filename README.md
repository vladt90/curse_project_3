# 🏛️ Система планирования культурных маршрутов

**Веб-приложение для построения туристических маршрутов по объектам культурного наследия города Москвы**

![Status](https://img.shields.io/badge/status-ready-success)
![Python](https://img.shields.io/badge/python-3.10+-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-green)
![MySQL](https://img.shields.io/badge/MySQL-8.x-orange)

---

## 📋 Описание проекта

Система предназначена для **автоматизированного формирования пешеходных и туристических маршрутов** по объектам культурного наследия на основе открытых государственных данных города Москвы.

### Основные возможности

- 🗺️ **Интерактивная карта** с объектами культурного наследия
- 📍 **Выбор точки старта** кликом по карте
- 🎯 **Умный алгоритм** построения оптимального маршрута (жадный алгоритм ближайшего соседа)
- 📊 **Настройка параметров** маршрута (2-20 объектов, радиус до 5 км)
- 🏛️ **Подробная информация** о каждом объекте наследия
- 👤 **Авторизация и регистрация** пользователей
- 📝 **История маршрутов** с возможностью просмотра
- ⚡ **Spatial индексы MySQL** для быстрого поиска

---

## 🛠 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк
- **Python 3.10+** - язык программирования
- **MySQL 8.x** - база данных с пространственными индексами
- **PyJWT** - JWT авторизация
- **bcrypt** - безопасное хэширование паролей
- **uvicorn** - ASGI сервер

### Frontend
- **HTML5 + CSS3** - разметка и стилизация
- **JavaScript (Vanilla)** - без фреймворков
- **Leaflet.js** - интерактивная карта
- **OpenStreetMap** - тайлы карты
- **Fetch API** - взаимодействие с backend

### База данных
- **MySQL 8.x** с Spatial индексами
- **Типы данных:** POINT (SRID 4326)
- **Функции:** ST_Distance_Sphere, ST_GeomFromText
- **Индексация:** SPATIAL INDEX для быстрого поиска

---

## 📁 Структура проекта

```
curse_project_3_sem/
├── backend/                    # Backend API (FastAPI)
│   ├── routes/                # API эндпоинты
│   │   ├── auth.py           # Регистрация/авторизация
│   │   ├── objects.py        # Объекты наследия
│   │   └── routes.py         # Маршруты
│   ├── services/             # Бизнес-логика
│   │   ├── auth_service.py   # Сервис авторизации
│   │   └── route_service.py  # Алгоритм маршрутизации
│   ├── main.py               # Точка входа
│   ├── config.py             # Конфигурация
│   ├── database.py           # Подключение к БД
│   ├── models.py             # Pydantic модели
│   └── requirements.txt      # Зависимости
├── frontend/                  # Web интерфейс
│   ├── index.html            # Главная страница
│   ├── login.html            # Страница входа
│   ├── register.html         # Страница регистрации
│   ├── css/
│   │   └── style.css         # Стили
│   └── js/
│       ├── api.js            # API клиент
│       └── map.js            # Работа с картой
├── database/                  # База данных
│   ├── schema.sql            # SQL схема
│   ├── import_data.py        # Скрипт импорта
│   ├── config.py             # Настройки БД
│   ├── requirements.txt      # Зависимости
│   └── SETUP.md              # Инструкция по БД
├── data.json                  # Датасет (5000+ объектов)
├── meta.json                  # Паспорт датасета
├── SETUP_GUIDE.md            # Подробная инструкция
├── API_DOCUMENTATION.md      # Документация API
└── README.md                 # Этот файл
```

---

## 🚀 Быстрый старт

### 📋 Требования

- **Python 3.10+**
- **MySQL 8.x** (через XAMPP или отдельная установка)
- **Современный браузер** (Chrome, Firefox, Edge)

### ⚡ Установка (5 минут)

#### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd curse_project_3_sem
```

#### 2. Настройте базу данных

**Запустите MySQL через XAMPP**, затем создайте БД:

```sql
CREATE DATABASE heritage_routes CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

**Примените схему** (через phpMyAdmin или командную строку):
```bash
mysql -u root -p heritage_routes < database/schema.sql
```

#### 3. Импортируйте данные

```bash
cd database
pip install -r requirements.txt
python import_data.py
```

> Импорт ~5000 объектов займёт 1-3 минуты

#### 4. Запустите Backend

```bash
cd backend
pip install -r requirements.txt
python main.py
```

> Backend доступен на http://localhost:8000

#### 5. Откройте Frontend

**Вариант А** (рекомендуется): Используйте Live Server в VS Code

**Вариант Б**: Запустите HTTP сервер
```bash
cd frontend
python -m http.server 5500
```

> Frontend доступен на http://localhost:5500

#### 6. Войдите в систему

Откройте `http://localhost:5500/login.html`

**Тестовые данные:**
- Логин: `admin`
- Пароль: `admin123`

---

## 📖 Документация

- **[Руководство по установке](SETUP_GUIDE.md)** - детальная инструкция
- **[API Документация](API_DOCUMENTATION.md)** - описание эндпоинтов
- **[Swagger UI](http://localhost:8000/docs)** - интерактивная документация API

---

## 🗺️ Использование

### 1. Построение маршрута

1. **Выберите точку старта** - кликните на карте Москвы
2. **Укажите количество объектов** (2-20)
3. **Нажмите "Построить маршрут"**
4. **Просмотрите результат** - маршрут отобразится на карте

### 2. Просмотр объектов

- Кликните на маркер объекта для просмотра информации
- Прокрутите список объектов в правой панели
- Кликните на карточку объекта для центрирования карты

### 3. История маршрутов

Все построенные маршруты сохраняются и доступны в истории пользователя через API `GET /api/routes`

---

## 🔧 API Эндпоинты

### Аутентификация

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/register` | Регистрация нового пользователя |
| POST | `/api/login` | Авторизация пользователя |
| GET | `/api/me` | Получить текущего пользователя |

### Объекты культурного наследия

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/objects` | Список объектов (с фильтрами и пагинацией) |
| GET | `/api/objects/{id}` | Получить объект по ID |
| GET | `/api/districts` | Список районов |
| GET | `/api/object-types` | Типы объектов |

### Маршруты

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/route` | Построить новый маршрут |
| GET | `/api/routes` | История маршрутов пользователя |
| GET | `/api/routes/{id}` | Детали маршрута по ID |

> Полная документация: [API_DOCUMENTATION.md](API_DOCUMENTATION.md)

---

## 🧮 Алгоритм маршрутизации

Система использует **жадный алгоритм ближайшего соседа** (Greedy Nearest Neighbor):

1. **Поиск ближайших объектов** - ST_Distance_Sphere в радиусе 5 км
2. **Построение маршрута:**
   - Начинаем с точки старта
   - Выбираем ближайший непосещённый объект
   - Повторяем до достижения N объектов
3. **Оптимизация:** Spatial индексы MySQL для O(log n) поиска

**Преимущества:**
- ✅ Быстрая работа (< 100ms на маршрут)
- ✅ Оптимальное расстояние для пешеходных маршрутов
- ✅ Учёт реальных координат

---

## 🗃️ База данных

### Схема

```sql
heritage_objects  -- Объекты культурного наследия
├── id (PK)
├── global_id (UNIQUE)
├── name
├── address
├── district
├── object_type
├── location (POINT + SPATIAL INDEX)
└── ...

users  -- Пользователи
├── id (PK)
├── username (UNIQUE)
├── email (UNIQUE)
├── password_hash (bcrypt)
└── ...

routes  -- Маршруты
├── id (PK)
├── user_id (FK)
├── start_location (POINT)
├── total_distance
└── ...

route_objects  -- Связь маршрутов и объектов
├── id (PK)
├── route_id (FK)
├── object_id (FK)
├── sequence_number
└── distance_from_previous
```

### Spatial индексы

```sql
SPATIAL INDEX idx_location ON heritage_objects(location);
```

Использование:
```sql
SELECT * FROM heritage_objects
WHERE ST_Distance_Sphere(
    location, 
    ST_GeomFromText('POINT(37.6208 55.7539)', 4326)
) <= 5000
ORDER BY ST_Distance_Sphere(...) ASC
LIMIT 10;
```

---

## 💡 Функции

### ✅ Реализовано

- [x] Регистрация и авторизация (JWT)
- [x] Интерактивная карта (Leaflet)
- [x] Построение маршрутов (жадный алгоритм)
- [x] Пространственный поиск (MySQL Spatial)
- [x] История маршрутов
- [x] REST API с документацией
- [x] Responsive дизайн
- [x] Безопасность (bcrypt, prepared statements)

### 🔮 Возможные улучшения

- [ ] Экспорт маршрутов (GPX, KML)
- [ ] Оценка времени прохождения
- [ ] Фильтры по категориям объектов
- [ ] Избранные объекты
- [ ] Социальные функции (публикация маршрутов)
- [ ] Мобильное приложение
- [ ] Офлайн режим

---

## 🧪 Тестирование

### Swagger UI

Откройте http://localhost:8000/docs для интерактивного тестирования API

### Ручное тестирование

```bash
# 1. Вход
curl -X POST http://localhost:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 2. Построение маршрута
curl -X POST http://localhost:8000/api/route \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "start_location":{"latitude":55.7539,"longitude":37.6208},
    "objects_count":5
  }'
```

---

## ❗ Возможные проблемы

### MySQL не подключается
- Убедитесь, что MySQL запущен в XAMPP
- Проверьте `backend/config.py` - параметры подключения

### CORS ошибка
- Используйте Live Server или HTTP сервер
- НЕ открывайте HTML файлы напрямую (`file://`)

### Пустая база данных
```bash
cd database
python import_data.py
```

### Модули не найдены
```bash
pip install -r backend/requirements.txt
pip install -r database/requirements.txt
```

> Подробнее: [SETUP_GUIDE.md](SETUP_GUIDE.md)

---

## 📊 Статистика

- **Объектов в БД:** ~5000+ объектов культурного наследия
- **Районов:** 120+ районов Москвы
- **Типов объектов:** 10+ категорий
- **API эндпоинтов:** 11
- **Строк кода:** ~2500+

---

## 🎓 Технические детали

### Архитектура

```
┌─────────────┐      HTTP/REST      ┌─────────────┐
│   Frontend  │ ←─────────────────→ │   Backend   │
│  (Leaflet)  │                     │  (FastAPI)  │
└─────────────┘                     └──────┬──────┘
                                           │ SQL
                                           ↓
                                    ┌─────────────┐
                                    │    MySQL    │
                                    │  (Spatial)  │
                                    └─────────────┘
```

### Безопасность

- ✅ **Пароли:** bcrypt хэширование
- ✅ **Авторизация:** JWT токены (HS256)
- ✅ **SQL:** Prepared statements (защита от инъекций)
- ✅ **CORS:** Настроен для cross-origin
- ✅ **Валидация:** Pydantic модели

---

## 🤝 Вклад

Проект разработан как курсовая работа. Pull requests приветствуются!

---

## 📄 Лицензия

**Данные:** Открытые данные города Москвы ([data.mos.ru](https://data.mos.ru/))

---

## 📞 Контакты

**Курсовой проект 3 семестр**

---

## 🙏 Благодарности

- **data.mos.ru** - за открытые данные
- **OpenStreetMap** - за карты
- **Leaflet** - за библиотеку карт
- **FastAPI** - за отличный фреймворк

