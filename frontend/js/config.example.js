// Локальная конфигурация фронтенда.
// Скопируйте этот файл как `frontend/js/config.js` и заполните значения.
// `frontend/js/config.js` НЕ должен попадать в git (он добавлен в .gitignore).

window.APP_CONFIG = {
  // Публичный ключ Яндекс.Карт (его нельзя «спрятать» во фронтенде).
  // Рекомендуется ограничить ключ по домену/referrer в кабинете Яндекса.
  YANDEX_MAPS_API_KEY: "PUT_YOUR_KEY_HERE"
};

