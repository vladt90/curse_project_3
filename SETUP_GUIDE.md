# Руководство по установке и запуску

## 📋 Содержание
1. [Требования](#требования)
2. [Установка базы данных](#установка-базы-данных)
3. [Импорт данных](#импорт-данных)
4. [Запуск Backend](#запуск-backend)
5. [Запуск Frontend](#запуск-frontend)
6. [Тестирование](#тестирование)

---

## 🔧 Требования

### Программное обеспечение
- **Python 3.10+** (для backend и импорта данных)
- **MySQL 8.x** (через XAMPP или отдельная установка)
- **Современный браузер** (Chrome, Firefox, Edge)
- **Git** (для клонирования репозитория)

### XAMPP
- Убедитесь, что XAMPP установлен и запущен
- Apache и MySQL должны быть активны

---

## 📦 Установка базы данных

### Шаг 1: Запустите MySQL в XAMPP

1. Откройте **XAMPP Control Panel**
2. Нажмите **Start** для MySQL
3. Дождитесь зеленого индикатора

### Шаг 2: Создайте базу данных

1. Откройте браузер: `http://localhost/phpmyadmin`
2. Перейдите на вкладку **SQL**
3. Выполните команду:

```sql
CREATE DATABASE heritage_routes CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Шаг 3: Примените схему

1. В phpMyAdmin выберите базу `heritage_routes`
2. Перейдите на вкладку **SQL**
3. Откройте файл `database/schema.sql` в текстовом редакторе
4. Скопируйте весь текст и вставьте в phpMyAdmin
5. Нажмите **Вперёд** (Go)

### Проверка

Выполните запрос:
```sql
SHOW TABLES;
```

Должны появиться таблицы:
- `heritage_objects`
- `users`
- `routes`
- `route_objects`

---

## 📊 Импорт данных

### Шаг 1: Установите зависимости

Откройте командную строку в папке проекта:

```bash
cd database
pip install -r requirements.txt
```

### Шаг 2: Запустите импорт

```bash
python import_data.py
```

Процесс импорта займёт 1-3 минуты. Вы увидите прогресс в консоли.

### Ожидаемый результат

```
============================================================
ИМПОРТ ДАННЫХ О КУЛЬТУРНОМ НАСЛЕДИИ МОСКВЫ
============================================================
Загрузка данных из ../data.json...
Загружено XXXX объектов
✓ Подключение к MySQL установлено
Обработка объектов...
✓ Обработано YYYY из XXXX объектов
Начало импорта YYYY объектов...
✓ Импорт завершен: YYYY объектов импортировано

============================================================
СТАТИСТИКА БАЗЫ ДАННЫХ
============================================================
Всего объектов в базе: YYYY
...
```

---

## 🚀 Запуск Backend

### Шаг 1: Установите зависимости

```bash
cd backend
pip install -r requirements.txt
```

### Шаг 2: Запустите сервер

```bash
python main.py
```

Или через uvicorn:

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Ожидаемый результат

```
============================================================
🚀 Запуск Heritage Routes System v1.0.0
============================================================
✓ Пул соединений с MySQL создан
✓ Подключение к базе данных успешно
✓ Сервер запущен на http://localhost:8000
✓ Документация API: http://localhost:8000/docs
============================================================
```

### Проверка Backend

Откройте в браузере:
- **API Root:** http://localhost:8000
- **Swagger Docs:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

---

## 🌐 Запуск Frontend

### Вариант А: Через Live Server (рекомендуется)

1. Установите расширение **Live Server** в VS Code
2. Откройте файл `frontend/index.html`
3. Нажмите правой кнопкой → **Open with Live Server**

### Вариант Б: Через Python HTTP сервер

```bash
cd frontend
python -m http.server 5500
```

Откройте браузер: `http://localhost:5500`

### Вариант В: Напрямую через файл

Просто откройте `frontend/login.html` в браузере.

**Примечание:** При открытии напрямую могут быть проблемы с CORS. Рекомендуется использовать Live Server.

---

## 🧪 Тестирование

### 1. Тест авторизации

1. Откройте `http://localhost:5500/login.html`
2. Войдите в созданный вами аккаунт (или зарегистрируйтесь)
3. Должно произойти перенаправление на главную страницу

### 2. Тест построения маршрута

1. На главной странице кликните на карте (в пределах Москвы)
2. Появится зелёный маркер точки старта
3. Выберите количество объектов (например, 5)
4. Нажмите **"Построить маршрут"**
5. Должен появиться маршрут с объектами культурного наследия

### 3. Тест API напрямую

Используйте Swagger UI: `http://localhost:8000/docs`

**Примеры запросов:**

**POST /api/login**
```json
{
  "username": "YOUR_USERNAME",
  "password": "YOUR_PASSWORD"
}
```

**POST /api/route** (требуется токен)
```json
{
  "start_location": {
    "latitude": 55.7539,
    "longitude": 37.6208
  },
  "objects_count": 5
}
```

---

## ❗ Возможные проблемы

### MySQL не подключается

**Проблема:** `Error connecting to MySQL`

**Решение:**
1. Убедитесь, что MySQL запущен в XAMPP
2. Проверьте параметры в `backend/config.py`:
   ```python
   DB_HOST = "localhost"
   DB_USER = "root"
   DB_PASSWORD = ""
   DB_NAME = "heritage_routes"
   DB_PORT = 3306
   ```

### CORS ошибка в браузере

**Проблема:** `Access to fetch at ... has been blocked by CORS policy`

**Решение:**
- Используйте Live Server или Python HTTP сервер
- НЕ открывайте HTML файлы напрямую (file://)

### Модуль не найден

**Проблема:** `ModuleNotFoundError: No module named 'fastapi'`

**Решение:**
```bash
pip install -r backend/requirements.txt
```

### База данных пустая

**Проблема:** Нет объектов в маршрутах

**Решение:**
```bash
cd database
python import_data.py
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте все шаги установки
2. Убедитесь, что все сервисы запущены:
   - ✅ MySQL (XAMPP)
   - ✅ Backend (Python)
   - ✅ Frontend (Live Server)
3. Проверьте логи в консоли

---

## ✅ Checklist перед запуском

- [ ] XAMPP установлен и MySQL запущен
- [ ] База данных `heritage_routes` создана
- [ ] Схема применена (4 таблицы созданы)
- [ ] Данные импортированы (есть объекты в `heritage_objects`)
- [ ] Python зависимости установлены (`pip install -r requirements.txt`)
- [ ] Backend запущен на порту 8000
- [ ] Frontend открыт через Live Server или HTTP сервер
- [ ] Авторизация работает (вход в свой аккаунт)

