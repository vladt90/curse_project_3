### Backend env template
# Скопируйте этот файл как `backend/.env` и заполните значения.
# `backend/.env` НЕ должен попадать в git (он игнорируется).

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=heritage_routes
DB_PORT=3306

# App
DEBUG=true
APP_HOST=0.0.0.0
APP_PORT=8000

# Yandex
# Нужен ключ Яндекс.Геокодера (для backend), а не ключ JS-карт.
YANDEX_GEOCODER_API_KEY=PUT_YOUR_GEOCODER_KEY_HERE

# OpenRouter (LLM)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-3n-e2b-it:free
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

